{
  "name": "prem-predics",
  "version": "1.0.0",
  "private": true,
  "description": "Prem Predics web app packaged for Android with Capacitor.",
  "type": "module",
  "scripts": {
    "build": "node scripts/prepare-capacitor-web.js",
    "cap:init:android": "npm run build && npx cap add android",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npm run cap:sync && npx cap open android",
    "cap:copy": "npm run build && npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest"
  }
}
